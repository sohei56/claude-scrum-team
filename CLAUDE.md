# claude-scrum-team Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-02-25

## Active Technologies
- Bash 3.2+ (macOS/Linux default) + Claude Code CLI with Agent Teams (001-ai-scrum-team)
- JSON files in `.scrum/` directory (one file per concern) (001-ai-scrum-team)
- Bash 3.2+ (shell scripts, hooks), Python 3.9+ (TUI dashboard), Markdown with YAML frontmatter (agent/skill definitions) + Claude Code CLI with Agent Teams (`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`), Textual 8.x (TUI framework), watchdog (filesystem monitoring) (001-ai-scrum-team)
- JSON files in `.scrum/` directory (one file per concern), Markdown for requirements and design documents (001-ai-scrum-team)
- JSON files in `.scrum/` directory (one file per concern), Markdown for requirements and design documents (governed by `.design/catalog.md`) (001-ai-scrum-team)

- Bash 3.2+ (shell scripts), Markdown with YAML frontmatter (agent definitions) + Claude Code CLI (with Agent Teams experimental flag), awesome-claude-code-subagents catalog (001-ai-scrum-team)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Bash 3.2+ (shell scripts), Markdown with YAML frontmatter (agent definitions)

## Code Style

Bash 3.2+ (shell scripts), Markdown with YAML frontmatter (agent definitions): Follow standard conventions

## Recent Changes
- 001-ai-scrum-team: Added Bash 3.2+ (shell scripts, hooks), Python 3.9+ (TUI dashboard), Markdown with YAML frontmatter (agent/skill definitions) + Claude Code CLI with Agent Teams (`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`), Textual 8.x (TUI framework), watchdog (filesystem monitoring)
- 001-ai-scrum-team: Added Bash 3.2+ (shell scripts, hooks), Python 3.9+ (TUI dashboard), Markdown with YAML frontmatter (agent/skill definitions) + Claude Code CLI with Agent Teams (`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`), Textual 8.x (TUI framework), watchdog (filesystem monitoring)
- 001-ai-scrum-team: Added Bash 3.2+ (shell scripts, hooks), Python 3.9+ (TUI dashboard), Markdown with YAML frontmatter (agent/skill definitions) + Claude Code CLI with Agent Teams (`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`), Textual 8.x (TUI framework), watchdog (filesystem monitoring)


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
