[project]
name = "claude-scrum-team"
requires-python = ">=3.9"

[tool.ruff]
target-version = "py39"
line-length = 100
src = ["dashboard"]

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B", "SIM"]
ignore = []

[tool.ruff.lint.isort]
known-first-party = ["dashboard"]
